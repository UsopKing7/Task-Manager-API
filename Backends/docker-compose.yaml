services:
  redis-task-manager:
    container_name: redis-task-manager
    image: redis:alpine
    restart: always
    networks:
      - task-manager-network
    ports:
      - 6379:6379
    volumes: 
      - redis-data:/data

networks:
  task-manager-network:
    driver: bridge

volumes:
  redis-data:

